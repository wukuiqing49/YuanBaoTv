<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/xxx_page_bg">

    <!-- TODO 根据当前 Figma Frame 图层结构替换布局内容 -->
    <!-- 主布局优先 ConstraintLayout；禁止写死 Figma Frame 宽高 -->
    <!-- 所有 Figma 尺寸按 360dp 基准宽度换算后进入 dimens.xml，不要直接 px 转 dp -->
    <!-- 普通 TextView 默认 wrap_content；不要把 Figma Text 图层宽高直接写成固定 dp -->
    <!-- 顶部 Tab + 下方列表页面应使用 Tab 组件 + ViewPager2 + 子 Fragment，不要在单个 XML 中静态堆多页内容 -->
    <!-- 重复卡片 / 行 / 表单项必须抽成 RecyclerView item XML，不要静态复制 -->

</androidx.constraintlayout.widget.ConstraintLayout>
